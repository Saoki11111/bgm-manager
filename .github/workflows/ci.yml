name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    name: Build macOS app
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run checks
        run: ./scripts/check.sh
